generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Produto {
  id        Int      @id @default(autoincrement())
  nome      String
  preco     Float
  estoque   Int
  criadoEm  DateTime @default(now())
}